pipeline{
    agent any
     tools {
        maven 'maven-3.9.9'
    }
    stages{
        stage('cloning_git_repo'){
            steps{
                git 'https://github.com/zakics444/simple-java-maven-app.git'
            }
        }
        stage('building'){
            steps{
                sh 'mvn clean package'
            }
        }
        stage('release'){
            steps{
            archive 'target/*.jar'
            }
        }
    }
}
